# Adalat AI - dependencies
# pip install -r requirements.txt
# Requires Python >= 3.10 (prefer 3.11-3.12 for spaCy wheels; see pyproject.toml)

# PDF extraction
pymupdf>=1.24.0

# NLP
spacy>=3.8.2,<4
# Install model after: python -m spacy download en_core_web_sm

# Sentence embeddings & alignment
sentence-transformers>=3.4.0

# Tokenizers
tokenizers>=0.22
sentencepiece>=0.2.0
tiktoken>=0.9.0

# Transformers (for tokenizer loading)
transformers>=4.48.0
huggingface-hub>=0.30.0

# Data processing
datasets>=3.5.0
pandas>=2.2.0
pyarrow>=19.0.0
openpyxl>=3.1.0
pyyaml>=6.0

# Evaluation
sacrebleu>=2.5.0

# Testing
pytest>=9.0.0

# Training / local Apple Silicon
torch>=2.6.0
peft>=0.15.0

# MLX (Apple Silicon only -- primary local LLM LoRA path)
mlx>=0.20
mlx-lm>=0.20
